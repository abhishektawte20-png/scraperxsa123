# Fixtures

Sanitized HTML snapshots for behavioral tests go here, one per evidenced field, alongside the DOM evidence that produced them (never production company data). Empty until the first field is evidenced.
